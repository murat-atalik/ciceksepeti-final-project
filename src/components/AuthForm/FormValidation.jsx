/* import React, { useState } from 'react';

function FormValidation({ fieldName, required, requiredErr, format,formvalues }) {
  const [err, setErr] = useState('');
  switch (fieldName) {
    case 'password':
        formvalues.length()>8 && formvalues.length()<20?setErr('')  ?setErr(''):setErr('Şifreniz en az 8 ve en fazla 20 karakter uzunluğunda olmalı!')
        return aş
  
    default:
      break;
  }

  return err;
}

export default FormValidation;
 */
